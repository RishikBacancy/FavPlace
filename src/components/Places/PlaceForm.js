import React from "react";
import { Text, View, StyleSheet } from "react-native";

const PlaceForm = () => {
    return(
        <View>
            <Text>Add Place Form</Text>
        </View> 
    );
};

const styles = StyleSheet.create({

});

export default PlaceForm;